name: Spec Kit - Specify Feature

# Implements: Feature specification generation + Azure DevOps Description overwrite
# Deferred (Backlog): FR-004 (logging), FR-005 (failure surfacing), FR-006 (enhanced traceability),
#                     FR-007 (regeneration), FR-008 (debounce), FR-010 (performance SLO),
#                     FR-011 (assumption auto-insertion)
# See: specs/001-ado-github-spec/spec.md Backlog section

on:
  workflow_dispatch:
    inputs:
      feature_description:
        description: 'Feature description (what to build)'
        required: true
        type: string
      create_branch:
        description: 'Create feature branch automatically'
        required: false
        type: boolean
        default: true
      work_item_id:
        description: 'Azure DevOps Work Item ID (for Description overwrite)'
        required: false
        type: string
      ado_changed_by_user_id:
        description: 'Azure DevOps User ID who last changed the work item (for reassignment)'
        required: false
        type: string

jobs:
  specify-feature:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    outputs:
      spec_file: ${{ steps.create_feature.outputs.spec_file }}
      branch_name: ${{ steps.create_feature.outputs.branch_name }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Setup Python for Spec Kit
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: |
          echo "📦 Installing Python dependencies (openai for LLM scripts)..."
          pip install -q openai

      - name: Cache uv and Specify CLI
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/uv
            ~/.local/share/uv
            ~/.local/bin/specify
          key: ${{ runner.os }}-uv-specify-${{ hashFiles('**/pyproject.toml') }}
          restore-keys: |
            ${{ runner.os }}-uv-specify-

      - name: Install uv and Specify CLI
        run: |
          if ! command -v uv &> /dev/null; then
            echo "📦 Installing uv package manager..."
            curl -LsSf https://astral.sh/uv/install.sh | sh
          else
            echo "✅ uv already cached"
          fi
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

          export PATH="$HOME/.cargo/bin:$PATH"
          
          if ! command -v specify &> /dev/null; then
            echo "📦 Installing Specify CLI..."
            uv tool install specify-cli --from git+https://github.com/github/spec-kit.git
          else
            echo "✅ Specify CLI already cached"
          fi
          echo "$HOME/.local/bin" >> $GITHUB_PATH

          echo "✅ Dependencies ready"

      - name: Cache GitHub Copilot CLI
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-npm-copilot-v1
          restore-keys: |
            ${{ runner.os }}-npm-copilot-
            ${{ runner.os }}-npm-

      - name: Install GitHub Copilot CLI
        run: |
          echo "📦 Installing GitHub Copilot CLI..."
          npm install -g @github/copilot
          npx @github/copilot --version
          echo "✅ Copilot CLI installed"

      - name: Verify Spec Kit prompts
        run: |
          echo "🔍 Checking for Spec Kit prompts..."
          if [ ! -d ".github/prompts" ]; then
            echo "❌ ERROR: Spec Kit not initialized!"
            echo "Please run 'spec-kit-init.yml' workflow first"
            exit 1
          fi

          if [ ! -f ".github/prompts/speckit.specify.prompt.md" ]; then
            echo "❌ ERROR: speckit.specify.prompt.md not found!"
            exit 1
          fi

          echo "✅ Spec Kit prompts found"
          ls -la .github/prompts/speckit.*.prompt.md

      - name: Step 1 - Generate Short Name with Copilot
        id: generate_name
        env:
          GITHUB_TOKEN: ${{ secrets.COPILOT_TOKEN || secrets.GITHUB_TOKEN }}
          GH_TOKEN: ${{ secrets.COPILOT_TOKEN || secrets.GITHUB_TOKEN }}
          FEATURE_DESC_RAW: ${{ github.event.inputs.feature_description }}
        run: |
          export GITHUB_TOKEN="${GITHUB_TOKEN}"
          export GH_TOKEN="${GH_TOKEN}"

          if [ -z "$GITHUB_TOKEN" ]; then
            echo "❌ ERROR: No authentication token found!"
            exit 1
          fi

          # Remove backticks from feature_description to prevent bash command substitution
          # Backticks in markdown (e.g., `code`) are interpreted as command substitution in bash
          # Using environment variable prevents direct substitution in script
          FEATURE_DESC_CLEAN=$(echo "$FEATURE_DESC_RAW" | tr '`' "'")

          echo "🤖 Step 1: Generating short branch name..."
          echo ""
          echo "Feature: $FEATURE_DESC_CLEAN"
          echo ""

          # Ask Copilot directly with compact prompt
          npx @github/copilot --allow-all-tools -p "Generate a 2-4 word kebab-case branch name for: $FEATURE_DESC_CLEAN. Respond with ONLY the branch name." 2>&1 | tee short_name_output.txt

          # Extract the short name (first non-empty line before statistics)
          SHORT_NAME=$(head -1 short_name_output.txt | tr -d '`' | xargs)

          echo "Generated short name: $SHORT_NAME"
          echo "short_name=$SHORT_NAME" >> $GITHUB_OUTPUT

      - name: Find existing branch for work item
        id: find_branch
        if: github.event.inputs.work_item_id != ''
        run: |
          WORK_ITEM_ID="${{ github.event.inputs.work_item_id }}"
          echo "🔍 Searching for existing branch with commits containing AB#$WORK_ITEM_ID..."
          
          # Fetch all remote branches to search
          git fetch --all --quiet
          
          # Find last commit with AB#<work_item_id>
          LAST_COMMIT=$(git log --all --oneline --grep="AB#$WORK_ITEM_ID" --format="%h" | head -1)
          
          if [ -n "$LAST_COMMIT" ]; then
            echo "Found commit: $LAST_COMMIT"
            
            # Find branch containing this commit (exclude main and HEAD)
            EXISTING_BRANCH=$(git branch -r --contains $LAST_COMMIT 2>/dev/null | grep -v "main\|HEAD" | head -1 | sed 's|origin/||' | xargs)
            
            if [ -n "$EXISTING_BRANCH" ]; then
              echo "existing_branch=$EXISTING_BRANCH" >> $GITHUB_OUTPUT
              echo "✅ Found existing branch: $EXISTING_BRANCH (from commit $LAST_COMMIT)"
            else
              echo "ℹ️ Commit found but no feature branch contains it"
            fi
          else
            echo "ℹ️ No commits found with AB#$WORK_ITEM_ID - will create new branch"
          fi

      - name: Step 2 - Run create-new-feature script
        id: create_feature
        env:
          FEATURE_DESC_RAW: ${{ github.event.inputs.feature_description }}
        run: |
          export PATH="$HOME/.local/bin:$HOME/.cargo/bin:$PATH"

          SHORT_NAME="${{ steps.generate_name.outputs.short_name }}"
          EXISTING_BRANCH="${{ steps.find_branch.outputs.existing_branch }}"
          
          # Remove backticks from feature_description to prevent bash command substitution
          # Backticks in markdown (e.g., `code`) are interpreted as command substitution in bash
          # Using environment variable prevents direct substitution in script
          FEATURE_DESC_CLEAN=$(echo "$FEATURE_DESC_RAW" | tr '`' "'")

          # Configure git
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Check if we should use existing branch
          if [ -n "$EXISTING_BRANCH" ]; then
            echo "🔄 Step 2: Using existing branch: $EXISTING_BRANCH"
            echo ""
            
            # Checkout existing branch
            git fetch origin "$EXISTING_BRANCH"
            git checkout "$EXISTING_BRANCH"
            git pull origin "$EXISTING_BRANCH" --rebase
            
            # Find existing spec file
            BRANCH_NAME="$EXISTING_BRANCH"
            SPEC_FILE="$GITHUB_WORKSPACE/specs/$BRANCH_NAME/spec.md"
            FEATURE_DIR="$GITHUB_WORKSPACE/specs/$BRANCH_NAME"
            
            # Create directory if it doesn't exist (shouldn't happen, but be safe)
            mkdir -p "$FEATURE_DIR"
            
            echo "✅ Checked out existing branch: $BRANCH_NAME"
            echo "  Spec file: $SPEC_FILE"
            echo "  Feature dir: $FEATURE_DIR"
          else
            echo "🌱 Step 2: Creating new feature branch..."
            echo "Short name: $SHORT_NAME"
            echo "Description: $FEATURE_DESC_CLEAN"
            echo ""

            # Run the script with --json flag
            cd "$GITHUB_WORKSPACE"
            .specify/scripts/bash/create-new-feature.sh --json --short-name "$SHORT_NAME" "$FEATURE_DESC_CLEAN" > feature_output.json

            # Parse JSON output
            cat feature_output.json
            BRANCH_NAME=$(jq -r '.BRANCH_NAME' feature_output.json)
            SPEC_FILE=$(jq -r '.SPEC_FILE' feature_output.json)
            FEATURE_DIR=$(jq -r '.FEATURE_DIR' feature_output.json)

            echo ""
            echo "✅ Feature scaffolding created:"
            echo "  Branch: $BRANCH_NAME"
            echo "  Spec file: $SPEC_FILE"
            echo "  Feature dir: $FEATURE_DIR"
          fi

          # Use multiline output format for long paths
          {
            echo "branch_name=$BRANCH_NAME"
            echo "spec_file<<EOF"
            echo "$SPEC_FILE"
            echo "EOF"
            echo "feature_dir=$FEATURE_DIR"
          } >> $GITHUB_OUTPUT

      - name: Step 3-6 - Generate Specification with Copilot
        env:
          GITHUB_TOKEN: ${{ secrets.COPILOT_TOKEN || secrets.GITHUB_TOKEN }}
          GH_TOKEN: ${{ secrets.COPILOT_TOKEN || secrets.GITHUB_TOKEN }}
          GH_WORKFLOW_DISPATCH_PAT: ${{ secrets.GH_WORKFLOW_DISPATCH_PAT }}
          AZURE_OPENAI_API_KEY: ${{ secrets.AZURE_OPENAI_API_KEY }}
          FEATURE_DESC_RAW: ${{ github.event.inputs.feature_description }}
        run: |
          export GITHUB_TOKEN="${GITHUB_TOKEN}"
          export GH_TOKEN="${GH_TOKEN}"
          export PATH="$HOME/.local/bin:$HOME/.cargo/bin:$PATH"

          SPEC_FILE="${{ steps.create_feature.outputs.spec_file }}"
          BRANCH_NAME="${{ steps.create_feature.outputs.branch_name }}"
          FEATURE_DIR="${{ steps.create_feature.outputs.feature_dir }}"

          # Fallback: Derive FEATURE_DIR from SPEC_FILE if null
          if [ -z "$FEATURE_DIR" ] || [ "$FEATURE_DIR" = "null" ]; then
            FEATURE_DIR="$(dirname "$SPEC_FILE")"
            echo "ℹ️  FEATURE_DIR was null, derived from SPEC_FILE: $FEATURE_DIR"
          fi

          # Remove backticks from feature_description to prevent bash command substitution
          # Using environment variable prevents direct substitution in script
          FEATURE_DESC_CLEAN=$(echo "$FEATURE_DESC_RAW" | tr '`' "'")

          echo "📝 Step 3-6: Generating specification with Copilot..."
          echo ""

          # Save feature description to temp file to avoid special character issues
          FEATURE_DESC_FILE="$FEATURE_DIR/feature_description.txt"
          echo "$FEATURE_DESC_CLEAN" > "$FEATURE_DESC_FILE"
          echo "💾 Saved feature description to: $FEATURE_DESC_FILE"
          echo ""

          # Generate spec with Copilot - using run-copilot-with-prompt.sh script
          .specify/scripts/bash/run-copilot-with-prompt.sh speckit.specify \
            FEATURE_DESC_FILE="$FEATURE_DESC_FILE" \
            BRANCH_NAME="$BRANCH_NAME" \
            SPEC_FILE="$SPEC_FILE" \
            FEATURE_DIR="$FEATURE_DIR" \
            2>&1 | tee spec_output.txt

          # Copilot with --allow-all-tools may create files directly
          # Search entire specs directory for newly created spec.md files first, then any .md
          SPEC_DIR=$(dirname "$SPEC_FILE")
          CREATED_SPEC=$(find specs -name "spec.md" -type f -newer "$SPEC_FILE" ! -path "$SPEC_FILE" 2>/dev/null | head -1)
          
          # If no spec.md found, look for any other .md file (fallback)
          # Exclude checklists/ subdirectories to avoid picking up checklist files
          if [ -z "$CREATED_SPEC" ]; then
            CREATED_SPEC=$(find specs -name "*.md" -type f -newer "$SPEC_FILE" ! -path "$SPEC_FILE" ! -path "*/checklists/*" 2>/dev/null | head -1)
          fi

          if [ -n "$CREATED_SPEC" ] && [ -f "$CREATED_SPEC" ]; then
            echo "✅ Copilot created file: $CREATED_SPEC"
            CREATED_DIR=$(dirname "$CREATED_SPEC")
            echo "📋 Moving spec to: $SPEC_FILE"
            cat "$CREATED_SPEC" > "$SPEC_FILE"
            
            # Copy checklists directory if it exists
            if [ -d "$CREATED_DIR/checklists" ]; then
              echo "📋 Copying checklists directory"
              cp -r "$CREATED_DIR/checklists" "$SPEC_DIR/"
            fi
            
            echo "🧹 Removing Copilot-created directory: $CREATED_DIR"
            rm -rf "$CREATED_DIR"
          else
            echo "⚠️ Copilot didn't create file directly, extracting from output..."
            # Extract markdown from output
            if grep -q "<< 'EOF'" spec_output.txt; then
              sed -n "/cat > .* << 'EOF'/,/^EOF$/p" spec_output.txt | sed '1d;$d' > "$SPEC_FILE"
            else
              sed -n '/^# /,${/Total usage/q;p}' spec_output.txt > "$SPEC_FILE"
            fi
          fi

          # Check if Copilot asked clarification questions instead of generating spec
          # Use LLM to detect questions (robust to format variations)
          echo "🤖 Using LLM to detect if Copilot output contains clarification questions..."
          
          # Export AZURE_OPENAI_API_KEY to make it available in embedded Python script
          export AZURE_OPENAI_API_KEY="${AZURE_OPENAI_API_KEY}"
          
          # Try LLM detection, but fallback to simple pattern matching if it fails (e.g., firewall blocking)
          if ! .github/scripts/detect-questions-llm.sh > question_detection.json 2>&1; then
            echo "⚠️  LLM detection failed (may be due to firewall/network restrictions)"
            echo "Detection output:"
            cat question_detection.json || echo "(no output file)"
            echo ""
            echo "🔄 Falling back to simple pattern-based detection..."
            
            # Fallback: Simple pattern matching for common question patterns
            if grep -qiE "(what|how|which|when|where|why|can you|do you|is there|are there|would you|could you)" spec_output.txt && \
               grep -qiE "(\?|question|clarify|clarification|need more|more information|please specify)" spec_output.txt; then
              echo '{"has_questions": true, "reason": "Fallback: Detected question patterns in Copilot output"}' > question_detection.json
            else
              echo '{"has_questions": false, "reason": "Fallback: No question patterns detected"}' > question_detection.json
            fi
          fi
          
          echo "✅ Question detection completed"
          echo "Detection result:"
          cat question_detection.json
          
          HAS_QUESTIONS=$(python3 -c "import json; print(json.load(open('question_detection.json'))['has_questions'])")
          DETECTION_REASON=$(python3 -c "import json; print(json.load(open('question_detection.json'))['reason'])")
          
          if [ "$HAS_QUESTIONS" = "True" ]; then
            echo "ℹ️  Copilot asked clarification questions: $DETECTION_REASON"
            echo "📝 Saving questions to spec file with markers..."
            
            # Save entire Copilot output to spec file with clarification header
            {
              echo "# Feature Specification: ${BRANCH_NAME}"
              echo ""
              echo "This feature requires clarification before implementation."
              echo ""
              echo "[NEEDS CLARIFICATION: Questions from Copilot]"
              echo ""
              cat spec_output.txt | sed '/^Total usage/,$d'
            } > "$SPEC_FILE"
            
            echo "✅ Clarification questions saved to: $SPEC_FILE"
          else
            # Normal spec generation path - validate spec file
            if [ ! -f "$SPEC_FILE" ]; then
              echo "❌ ERROR: Spec file was not created: $SPEC_FILE"
              echo "Copilot output:"
              cat spec_output.txt
              exit 1
            fi

            SPEC_SIZE=$(wc -c < "$SPEC_FILE")
            if [ "$SPEC_SIZE" -lt 100 ]; then
              echo "❌ ERROR: Spec file is too small ($SPEC_SIZE bytes), generation likely failed"
              echo "Spec file content:"
              cat "$SPEC_FILE"
              echo ""
              echo "Copilot output:"
              cat spec_output.txt
              exit 1
            fi

            if ! grep -q "^# " "$SPEC_FILE"; then
              echo "❌ ERROR: Spec file doesn't contain markdown headers"
              echo "Spec file content:"
              cat "$SPEC_FILE"
              exit 1
            fi

            echo ""
            echo "✅ Specification saved to: $SPEC_FILE"
            echo ""
            echo "Preview (first 30 lines):"
            head -30 "$SPEC_FILE"
          fi

      # T004-T006: Foundational - Marker Detection
      - name: Detect Clarification Markers
        id: detect_markers
        run: |
          SPEC_FILE="${{ steps.create_feature.outputs.spec_file }}"
          
          if grep -q '\[NEEDS CLARIFICATION:' "$SPEC_FILE"; then
            echo "markers_found=true" >> $GITHUB_OUTPUT
            MARKER_COUNT=$(grep -c '\[NEEDS CLARIFICATION:' "$SPEC_FILE" || echo "0")
            echo "marker_count=$MARKER_COUNT" >> $GITHUB_OUTPUT
            echo "✅ Found $MARKER_COUNT clarification markers"
          else
            echo "markers_found=false" >> $GITHUB_OUTPUT
            echo "marker_count=0" >> $GITHUB_OUTPUT
            echo "ℹ️  No clarification markers found - clear requirements"
          fi

      # T007-T010: User Story 1 - Preserve markers (already preserved by default, add logging)
      # T018-T019: User Story 2 - Extract Clarifications (using LLM for robust parsing)
      - name: Extract Clarifications
        if: steps.detect_markers.outputs.markers_found == 'true'
        env:
          GH_WORKFLOW_DISPATCH_PAT: ${{ secrets.GH_WORKFLOW_DISPATCH_PAT }}
          GITHUB_TOKEN: ${{ secrets.COPILOT_TOKEN || secrets.GITHUB_TOKEN }}
          AZURE_OPENAI_API_KEY: ${{ secrets.AZURE_OPENAI_API_KEY }}
        run: |
          export PATH="$HOME/.local/bin:$HOME/.cargo/bin:$PATH"
          
          SPEC_FILE="${{ steps.create_feature.outputs.spec_file }}"
          FEATURE_DIR="${{ steps.create_feature.outputs.feature_dir }}"
          BRANCH_NAME="${{ steps.create_feature.outputs.branch_name }}"
          
          # Fallback: Derive FEATURE_DIR from SPEC_FILE if null
          if [ -z "$FEATURE_DIR" ] || [ "$FEATURE_DIR" = "null" ]; then
            FEATURE_DIR="$(dirname "$SPEC_FILE")"
            echo "ℹ️  FEATURE_DIR was null, derived from SPEC_FILE: $FEATURE_DIR"
          fi
          
          echo "📝 Extracting clarification questions using LLM..."
          python3 .github/scripts/extract-clarifications-llm.py \
            --spec-file "$SPEC_FILE" \
            --output "$FEATURE_DIR/clarifications.md" \
            --feature-name "$BRANCH_NAME"

      # T031-T032: User Story 3 - Create ADO Issues for Clarifications
      - name: Create ADO Issues for Clarifications
        if: steps.detect_markers.outputs.markers_found == 'true' && github.event.inputs.work_item_id != ''
        env:
          ADO_WORK_ITEM_PAT: ${{ secrets.ADO_WORKITEM_RW_PAT }}
          ADO_ORG_URL: https://dev.azure.com/${{ vars.ADO_ORG || 'your-org' }}
          ADO_PROJECT: ${{ vars.ADO_PROJECT || 'your-project' }}
          FEATURE_ID: ${{ github.event.inputs.work_item_id }}
          BRANCH_NAME: ${{ steps.create_feature.outputs.branch_name }}
          AZURE_OPENAI_API_KEY: ${{ secrets.AZURE_OPENAI_API_KEY }}
        run: |
          SPEC_FILE="${{ steps.create_feature.outputs.spec_file }}"
          FEATURE_DIR="${{ steps.create_feature.outputs.feature_dir }}"
          
          # Fallback: Derive FEATURE_DIR from SPEC_FILE if null
          if [ -z "$FEATURE_DIR" ] || [ "$FEATURE_DIR" = "null" ]; then
            FEATURE_DIR="$(dirname "$SPEC_FILE")"
            echo "ℹ️  FEATURE_DIR was null, derived from SPEC_FILE: $FEATURE_DIR"
          fi
          
          # Use spec_output.txt from Copilot step (contains full formatted questions)
          SPEC_OUTPUT_FILE="spec_output.txt"
          QUESTIONS_JSON="$FEATURE_DIR/questions.json"
          
          if [ ! -f "$SPEC_OUTPUT_FILE" ]; then
            echo "⚠️ spec_output.txt not found, skipping Issue creation"
            exit 0
          fi
          
          # Extract questions using LLM (reuse existing implementation)
          echo "📡 Extracting questions from Copilot output using LLM..."
          python3 .github/scripts/extract-clarifications-llm.py \
            --spec-output-file "$SPEC_OUTPUT_FILE" \
            --output "$QUESTIONS_JSON"
          
          if [ ! -f "$QUESTIONS_JSON" ]; then
            echo "⚠️ No questions extracted, skipping Issue creation"
            exit 0
          fi
          
          # Install Python dependencies for ado_client.py
            echo "📦 Installing Python dependencies..."
            pip install -q requests openai html2text
          
          echo "🎫 Creating ADO Issues for clarification questions..."
          .github/scripts/create-ado-issues.sh \
            --questions-json "$QUESTIONS_JSON" \
            --feature-id "$FEATURE_ID" \
            --branch "$BRANCH_NAME" \
            --org-url "$ADO_ORG_URL" \
            --project "$ADO_PROJECT"

      - name: Step 7 - Commit and Push Specification
        if: github.event.inputs.create_branch == 'true'
        run: |
          BRANCH_NAME="${{ steps.create_feature.outputs.branch_name }}"
          SPEC_FILE="${{ steps.create_feature.outputs.spec_file }}"

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          echo "📤 Step 7: Committing and pushing specification..."
          echo "Branch: $BRANCH_NAME"
          echo "Spec file: $SPEC_FILE"
          echo ""

          # Ensure we're on the correct branch (Copilot may have switched branches)
          CURRENT_BRANCH=$(git branch --show-current)
          if [ "$CURRENT_BRANCH" != "$BRANCH_NAME" ]; then
            echo "⚠️  Currently on branch '$CURRENT_BRANCH', switching to '$BRANCH_NAME'"
            git checkout "$BRANCH_NAME"
          fi

          # Add all generated files
          git add -A

          if git diff --staged --quiet; then
            echo "⚠️ No changes to commit"
          else
            echo "💾 Committing specification..."
            
            # Build commit message with optional AB# work item reference
            WORK_ITEM_ID="${{ github.event.inputs.work_item_id }}"
            if [ -n "$WORK_ITEM_ID" ]; then
              COMMIT_MSG="feat: add feature specification AB#$WORK_ITEM_ID"
            else
              COMMIT_MSG="feat: add feature specification"
            fi
            
            git commit -m "$COMMIT_MSG" \
              -m "Feature: ${{ github.event.inputs.feature_description }}" \
              -m "Generated by /speckit.specify workflow" \
              -m "Spec file: $SPEC_FILE"

            echo "📤 Pushing to branch: $BRANCH_NAME"
            git push origin "$BRANCH_NAME"

            echo "✅ Specification committed and pushed to $BRANCH_NAME"
            if [ -n "$WORK_ITEM_ID" ]; then
              echo "✅ Commit includes AB#$WORK_ITEM_ID - Azure Boards will create Development tab link"
            fi
          fi

      # ADO Description Overwrite (T007, T008, T009)
      # Contract reference: specs/001-ado-github-spec/contracts/README.md
      # Implements JSON Patch REPLACE operation with exponential backoff retry
      # Always update description if spec file exists and is valid, even if clarification markers are present
      - name: Overwrite ADO Work Item Description
        if: github.event.inputs.work_item_id != ''
        shell: bash
        env:
          ADO_WORKITEM_RW_PAT: ${{ secrets.ADO_WORKITEM_RW_PAT }}
          ADO_ORG: ${{ vars.ADO_ORG || 'your-org' }}
          ADO_PROJECT: ${{ vars.ADO_PROJECT || 'your-project' }}
        run: |
          set +e  # Disable exit on error, handle errors manually
          
          WORK_ITEM_ID="${{ github.event.inputs.work_item_id }}"
          SPEC_FILE="${{ steps.create_feature.outputs.spec_file }}"

          echo "=== ADO Description Update Debug Info ==="
          echo "WORK_ITEM_ID: $WORK_ITEM_ID"
          echo "SPEC_FILE: $SPEC_FILE"
          echo "ADO_ORG: $ADO_ORG"
          echo "ADO_PROJECT: $ADO_PROJECT"
          echo "ADO_WORKITEM_RW_PAT length: ${#ADO_WORKITEM_RW_PAT}"
          echo ""

          if [ -z "$ADO_WORKITEM_RW_PAT" ]; then
            echo "⚠️ ADO_WORKITEM_RW_PAT not set; skipping Description overwrite"
            exit 0
          fi

          if [ "$ADO_ORG" = "your-org" ] || [ "$ADO_PROJECT" = "your-project" ]; then
            echo "❌ ERROR: ADO_ORG or ADO_PROJECT not configured!"
            echo "Please set GitHub repository variables:"
            echo "  - ADO_ORG: Your Azure DevOps organization name"
            echo "  - ADO_PROJECT: Your Azure DevOps project name"
            exit 1
          fi

          echo "🔄 Overwriting ADO Work Item #$WORK_ITEM_ID Description with generated spec..."

          # Check if spec file exists
          if [ ! -f "$SPEC_FILE" ]; then
            echo "❌ Spec file not found: $SPEC_FILE"
            exit 1
          fi

          # Read spec content and validate it's not empty
          SPEC_CONTENT=$(<"$SPEC_FILE")
          FILE_SIZE=$(wc -c < "$SPEC_FILE")
          echo "📄 Spec file size: $FILE_SIZE bytes"
          
          # Validate spec has meaningful content (at least 100 bytes and contains markdown header)
          if [ "$FILE_SIZE" -lt 100 ]; then
            echo "❌ Spec file too small ($FILE_SIZE bytes); likely empty or generation failed"
            exit 1
          fi
          
          if ! grep -q "^# " "$SPEC_FILE"; then
            echo "❌ Spec file doesn't contain markdown headers; likely invalid"
            exit 1
          fi
          
          SPEC_JSON=$(jq -Rs . <<< "$SPEC_CONTENT")
          if [ $? -ne 0 ]; then
            echo "❌ Failed to JSON-encode spec content"
            exit 1
          fi

          # Construct JSON Patch payload
          PAYLOAD=$(cat <<EOF
          [
            {
              "op": "replace",
              "path": "/fields/System.Description",
              "value": $SPEC_JSON
            }
          ]
          EOF
          )

          # PATCH with retry (x2 exponential backoff)
          ENDPOINT="https://dev.azure.com/${ADO_ORG}/${ADO_PROJECT}/_apis/wit/workitems/${WORK_ITEM_ID}?api-version=7.0"
          
          for ATTEMPT in 1 2 3; do
            echo ""
            echo "=== Attempt $ATTEMPT/3 ==="
            echo "Endpoint: $ENDPOINT"
            echo "Payload size: ${#PAYLOAD} chars"
            
            RESPONSE=$(curl -v -s -w "\n%{http_code}" -X PATCH \
              --http1.1 \
              --max-time 30 \
              --connect-timeout 10 \
              -H "Content-Type: application/json-patch+json" \
              -H "Authorization: Basic $(echo -n ":${ADO_WORKITEM_RW_PAT}" | base64 | tr -d '\n')" \
              -d "$PAYLOAD" \
              "$ENDPOINT" 2>&1)
            
            HTTP_CODE=$(echo "$RESPONSE" | tail -1)
            BODY=$(echo "$RESPONSE" | sed '$d')
            
            echo "HTTP Status: $HTTP_CODE"
            
            if [ "$HTTP_CODE" = "200" ] || [ "$HTTP_CODE" = "204" ]; then
              echo "✅ ADO Description overwritten successfully (HTTP $HTTP_CODE)"
              exit 0
            elif [ "$HTTP_CODE" = "401" ]; then
              echo "❌ Authentication failed (HTTP 401)"
              echo "Possible causes:"
              echo "  - Invalid or expired ADO_WORKITEM_RW_PAT"
              echo "  - PAT doesn't have 'Work Items: Read & Write' scope"
              echo "Response: $BODY"
              exit 1
            elif [ "$HTTP_CODE" = "403" ]; then
              echo "❌ Authorization failed (HTTP 403)"
              echo "Possible causes:"
              echo "  - PAT doesn't have permission to update work items"
              echo "  - Work item is in a protected area"
              echo "Response: $BODY"
              exit 1
            elif [ "$HTTP_CODE" = "404" ]; then
              echo "❌ Work item not found (HTTP 404)"
              echo "Possible causes:"
              echo "  - Work item ID $WORK_ITEM_ID doesn't exist"
              echo "  - Wrong ADO_ORG or ADO_PROJECT"
              echo "Response: $BODY"
              exit 1
            elif [[ "$HTTP_CODE" =~ ^4 ]]; then
              echo "❌ Client error (HTTP $HTTP_CODE); not retrying"
              echo "Response body:"
              echo "$BODY" | head -n 50
              exit 1
            else
              echo "⚠️ Server error (HTTP $HTTP_CODE); retrying after backoff..."
              echo "Response: $BODY" | head -n 20
              [ $ATTEMPT -lt 3 ] && sleep $((2 ** ATTEMPT))
            fi
          done
          
          echo ""
          echo "❌ Failed after 3 attempts"
          exit 1

      - name: Debug Work Item Assignment Inputs
        if: github.event.inputs.work_item_id != ''
        shell: bash
        run: |
          echo "=== Debug: Work Item Assignment Inputs ==="
          WORK_ITEM_ID="${{ github.event.inputs.work_item_id }}"
          CHANGED_BY_USER_ID="${{ github.event.inputs.ado_changed_by_user_id }}"
          echo "work_item_id: '$WORK_ITEM_ID'"
          echo "ado_changed_by_user_id: '$CHANGED_BY_USER_ID'"
          echo "ado_changed_by_user_id length: ${#CHANGED_BY_USER_ID}"
          echo ""
          if [ -z "$CHANGED_BY_USER_ID" ]; then
            echo "⚠️ WARNING: ado_changed_by_user_id is empty or not provided"
            echo "This step will be skipped because the condition requires both work_item_id and ado_changed_by_user_id"
            echo ""
            echo "Possible reasons:"
            echo "  1. Workflow was triggered manually without providing ado_changed_by_user_id"
            echo "  2. Azure Function did not extract ChangedBy user email from webhook payload"
            echo "  3. ChangedBy user email was not found in ADO work item"
          else
            echo "✅ ado_changed_by_user_id is provided: $CHANGED_BY_USER_ID"
          fi
      
      - name: Assign Work Item to Changed By User
        if: github.event.inputs.work_item_id != '' && github.event.inputs.ado_changed_by_user_id != ''
        shell: bash
        env:
          ADO_WORKITEM_RW_PAT: ${{ secrets.ADO_WORKITEM_RW_PAT }}
          ADO_ORG: ${{ vars.ADO_ORG || 'your-org' }}
          ADO_PROJECT: ${{ vars.ADO_PROJECT || 'your-project' }}
        run: |
          set +e  # Disable exit on error, handle errors manually
          
          WORK_ITEM_ID="${{ github.event.inputs.work_item_id }}"
          CHANGED_BY_USER_ID="${{ github.event.inputs.ado_changed_by_user_id }}"

          echo "=== ADO Work Item Assignment Debug Info ==="
          echo "WORK_ITEM_ID: $WORK_ITEM_ID"
          echo "CHANGED_BY_USER_ID: $CHANGED_BY_USER_ID"
          echo "ADO_ORG: $ADO_ORG"
          echo "ADO_PROJECT: $ADO_PROJECT"
          echo "ADO_WORKITEM_RW_PAT length: ${#ADO_WORKITEM_RW_PAT}"
          echo ""

          if [ -z "$ADO_WORKITEM_RW_PAT" ]; then
            echo "⚠️ ADO_WORKITEM_RW_PAT not set; skipping work item assignment"
            exit 0
          fi

          if [ "$ADO_ORG" = "your-org" ] || [ "$ADO_PROJECT" = "your-project" ]; then
            echo "❌ ERROR: ADO_ORG or ADO_PROJECT not configured!"
            echo "Please set GitHub repository variables:"
            echo "  - ADO_ORG: Your Azure DevOps organization name"
            echo "  - ADO_PROJECT: Your Azure DevOps project name"
            exit 1
          fi

          echo "🔄 Assigning ADO Work Item #$WORK_ITEM_ID to user $CHANGED_BY_USER_ID..."

          # Construct JSON Patch payload to assign work item
          PAYLOAD=$(cat <<EOF
          [
            {
              "op": "replace",
              "path": "/fields/System.AssignedTo",
              "value": "$CHANGED_BY_USER_ID"
            }
          ]
          EOF
          )

          # PATCH with retry (exponential backoff)
          ENDPOINT="https://dev.azure.com/${ADO_ORG}/${ADO_PROJECT}/_apis/wit/workitems/${WORK_ITEM_ID}?api-version=7.0"
          
          for ATTEMPT in 1 2 3; do
            echo ""
            echo "=== Attempt $ATTEMPT/3 ==="
            echo "Endpoint: $ENDPOINT"
            echo "Payload: $PAYLOAD"
            
            RESPONSE=$(curl -s -w "\n%{http_code}" -X PATCH \
              --http1.1 \
              --max-time 30 \
              --connect-timeout 10 \
              -H "Content-Type: application/json-patch+json" \
              -H "Authorization: Basic $(echo -n ":${ADO_WORKITEM_RW_PAT}" | base64 | tr -d '\n')" \
              -d "$PAYLOAD" \
              "$ENDPOINT" 2>&1)
            
            HTTP_CODE=$(echo "$RESPONSE" | tail -1)
            BODY=$(echo "$RESPONSE" | sed '$d')
            
            echo "HTTP Status: $HTTP_CODE"
            
            if [ "$HTTP_CODE" = "200" ] || [ "$HTTP_CODE" = "204" ]; then
              echo "✅ ADO Work Item assigned successfully (HTTP $HTTP_CODE)"
              exit 0
            elif [ "$HTTP_CODE" = "401" ]; then
              echo "❌ Authentication failed (HTTP 401)"
              echo "Possible causes:"
              echo "  - Invalid or expired ADO_WORKITEM_RW_PAT"
              echo "  - PAT doesn't have 'Work Items: Read & Write' scope"
              echo "Response: $BODY"
              exit 1
            elif [ "$HTTP_CODE" = "403" ]; then
              echo "❌ Authorization failed (HTTP 403)"
              echo "Possible causes:"
              echo "  - PAT doesn't have permission to update work items"
              echo "  - Work item is in a protected area"
              echo "Response: $BODY"
              exit 1
            elif [ "$HTTP_CODE" = "404" ]; then
              echo "❌ Work item not found (HTTP 404)"
              echo "Possible causes:"
              echo "  - Work item ID $WORK_ITEM_ID doesn't exist"
              echo "  - Wrong ADO_ORG or ADO_PROJECT"
              echo "Response: $BODY"
              exit 1
            elif [[ "$HTTP_CODE" =~ ^4 ]]; then
              echo "❌ Client error (HTTP $HTTP_CODE); not retrying"
              echo "Response body:"
              echo "$BODY" | head -n 50
              exit 1
            else
              echo "⚠️ Server error (HTTP $HTTP_CODE); retrying after backoff..."
              echo "Response: $BODY" | head -n 20
              [ $ATTEMPT -lt 3 ] && sleep $((2 ** ATTEMPT))
            fi
          done
          
          echo ""
          echo "❌ Failed after 3 attempts"
          exit 1

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: spec-kit-output
          path: |
            short_name_output.txt
            feature_output.json
            spec_prompt.txt
            spec_output.txt
            ${{ steps.create_feature.outputs.feature_dir }}
          retention-days: 30

      - name: Summary
        if: always()
        run: |
          BRANCH_NAME="${{ steps.create_feature.outputs.branch_name }}"
          SPEC_FILE="${{ steps.create_feature.outputs.spec_file }}"
          FEATURE_DIR="${{ steps.create_feature.outputs.feature_dir }}"
          SHORT_NAME="${{ steps.generate_name.outputs.short_name }}"
          MARKERS_FOUND="${{ steps.detect_markers.outputs.markers_found }}"
          MARKER_COUNT="${{ steps.detect_markers.outputs.marker_count }}"

          echo "## 📋 Spec Kit - Specify Feature" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Feature:** ${{ github.event.inputs.feature_description }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📊 Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Short Name:** \`$SHORT_NAME\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Branch:** \`$BRANCH_NAME\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Spec File:** \`$SPEC_FILE\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Feature Directory:** \`$FEATURE_DIR\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🔍 Clarification Status" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "$MARKERS_FOUND" = "true" ]; then
            echo "- **Markers Found:** ✅ Yes ($MARKER_COUNT markers)" >> $GITHUB_STEP_SUMMARY
            if [ -f "$FEATURE_DIR/clarifications.md" ]; then
              echo "- **Clarifications File:** ✅ Created at \`$FEATURE_DIR/clarifications.md\`" >> $GITHUB_STEP_SUMMARY
            else
              echo "- **Clarifications File:** ❌ Not created" >> $GITHUB_STEP_SUMMARY
            fi
            if [ -n "${{ github.event.inputs.work_item_id }}" ]; then
              echo "- **ADO Issues:** ✅ Created for Feature #${{ github.event.inputs.work_item_id }}" >> $GITHUB_STEP_SUMMARY
            else
              echo "- **ADO Issues:** ⏭️ Skipped (no work_item_id provided)" >> $GITHUB_STEP_SUMMARY
            fi
          else
            echo "- **Markers Found:** ✅ No - Clear requirements, no clarifications needed" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### ✅ Completed Steps" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "1. ✅ Generated short branch name using Copilot" >> $GITHUB_STEP_SUMMARY
          echo "2. ✅ Created feature scaffolding with \`create-new-feature.sh\`" >> $GITHUB_STEP_SUMMARY
          echo "3. ✅ Generated specification using Copilot + spec template" >> $GITHUB_STEP_SUMMARY
          echo "4. ✅ Committed and pushed to feature branch" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📖 Next Steps" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "1. Review the generated specification in the feature branch" >> $GITHUB_STEP_SUMMARY
          echo "2. Run \`/speckit.clarify\` if there are [NEEDS CLARIFICATION] markers" >> $GITHUB_STEP_SUMMARY
          echo "3. Run \`/speckit.plan\` to create technical implementation plan" >> $GITHUB_STEP_SUMMARY
          echo "4. Run \`/speckit.tasks\` to generate actionable task lists" >> $GITHUB_STEP_SUMMARY
          echo "5. Run \`/speckit.implement\` to execute all tasks" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🔗 Resources" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- [View Branch](https://github.com/${{ github.repository }}/tree/$BRANCH_NAME)" >> $GITHUB_STEP_SUMMARY
          echo "- [View Spec](https://github.com/${{ github.repository }}/blob/$BRANCH_NAME/$SPEC_FILE)" >> $GITHUB_STEP_SUMMARY
          echo "- [Spec Kit Documentation](https://github.github.io/spec-kit/)" >> $GITHUB_STEP_SUMMARY
