# Simple Mermaid Test

```mermaid
graph LR
    A[Start] --> B[Process]
    B --> C[End]
```

If you can see a diagram above with three boxes (Start, Process, End) connected by arrows, then Mermaid is working.
